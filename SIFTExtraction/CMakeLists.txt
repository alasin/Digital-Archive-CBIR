cmake_minimum_required(VERSION 2.8)
project( createVocabulary )
find_package( OpenCV REQUIRED )
add_executable( createVocabulary createvocab.cpp)
target_link_libraries( createVocabulary ${OpenCV_LIBS} )
